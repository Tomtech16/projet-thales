#ici se trouvera le code python permettant d'extraire des bonnes pratiques que l'on souhaite avoir.
import sqlite3
conn = sqlite3.connect('bdd.db') #pour se connecter à la base de données.
query = """
SELECT *
FROM bonnes_pratiques
WHERE site_web = 'votre_site_web'
"""
....

conn.close()


print('PDF créé avec succès!')

# Sauvegarde des données dans un fichier CSV
df.to_csv('bonnes_pratiques_extraites.csv', index=False)
